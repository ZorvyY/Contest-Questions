#define FOR(i,a,b) for(int i = a; i < b; i++)
#define SCD(a) scanf("%d", &a)
#include <iostream>
#include <stdio.h>

using namespace std;

int main(void)
{
    FOR(i,0,11) {
        cout << i << " ";
    }

    return 0;
}
