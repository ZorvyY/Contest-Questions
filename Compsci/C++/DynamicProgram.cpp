#include <iostream>

using namespace std;

int segsum (int &arrayy[], int start, int seg, int length) {
    if (seg = 0) {
        result sum;
    }
}

int main () {
    int N;
    cin >> N;
    int seg
    cin >> seg;
    int nums[N];
    for (int i = 0; i < N; i++) {
        cin >> nums[i];
    }


    return 0;
}
